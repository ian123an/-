#include<iostream>
#include<iomanip>
#include<string>
#include"Score.h"
using namespace std;

int main()
{
	string n, x;
	int m, y;
	cin >> n >> m ;
	Score score1("John", 90);
	Score score2("Mary", 85);

	score1.setName(n);
	score2.setMath(m);

	score1.print();
	score2.print();
	return 0;
}
