from Shape import Shape
from Sphere import Sphere
from Cuboid import Cuboid
list=[]
for i in range(6):
    list.append(input())
a = Sphere(int(list[0]))
b = Shape(list[1])
c = Cuboid(int(list[2]),int(list[3]),int(list[4]))
f = Shape(list[5])
print(str(a),":(",a.getRadius(),"),",round(a.getVolume(),1),",",b.getColor())
print(str(c),":(",c.getLength(),",",c.getWidth(),",",c.getHeight(),"),",c.getVolume(),",",f.getColor(),sep='')
